// Re-export the fhir-kit-client so consumers can use it directly via our clients API surface
export { default as FhirClient } from 'fhir-kit-client';
// Note: fhir-kit-client provides TypeScript declarations; consumers can rely on them via this re-export.

